name: Publish package

on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest

    env:
      NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      NPM_CONFIG_OTP: ""

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js for npm
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org/

      - name: Install dependencies
        run: npm ci

      - name: Auto-bump version from npm before publish
        run: |
          npm run version:auto
          npm install --package-lock-only
          echo "Publishing version: $(node -p \"require('./package.json').version\")"

      - name: Build package
        run: npm run build

      - name: Validate npm authentication token
        run: |
          if [ -z "$NODE_AUTH_TOKEN" ]; then
            echo "::error::NPM_TOKEN secret is not configured. Add repository secret NPM_TOKEN using an npm Automation token."
            exit 1
          fi
          npm config get registry
          npm whoami

      - name: Dry-run publish validation
        run: npm run publish:dry-run

      - name: Publish to npm
        run: npm publish ./dist/enterprise-components --access public --provenance --otp=""
